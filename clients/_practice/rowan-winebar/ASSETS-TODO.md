# Before upload — 2 assets to add to this folder

1. `og-share.webp` — 1200×630 social-share image (screenshot the hero at 1200×630 or generate one). Referenced by og:image/twitter:image in index.html.
2. Real photography set (replaces the labeled PLACEHOLDER tiles) — see the image plan in the build README.

Also swap `TODO-DOMAIN.ca` for the real domain in: index.html (canonical + og/twitter URLs), robots.txt, sitemap.xml.
