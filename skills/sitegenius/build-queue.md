# Practice Build Queue — exercising the catalog

Goal: touch every register × tier combo once to graduate catalog recipes from documented to verified. Not real clients — throwaway practice builds in `clients/_practice/`.

- [x] 1. LocalSite, trust-first — accounting firm (baseline template, unmodified motion layer) — DONE (`clients/_practice/accounting-localsite/`), found+fixed grid overflow bug
- [ ] 2. AwardsSite, trust-first — immigration/paralegal — **DONE** (`clients/immigration-paralegal/`)
- [x] 3. AwardsSite, warm-local — restaurant (Trattoria Marchetti) — DONE (`clients/_practice/trattoria-awardssite/`), swapped WebGL aura for photo hero + draggable gallery, found+fixed dangling ScrollTrigger ref + copy-miss gotchas
- [x] 4. AwardsSite, craft/impact — trades (Ironclad Roofing) — DONE (`clients/_practice/ironclad-roofing/`), spotlight-cursor before/after centerpiece, Archivo Expanded (bold not italic — no italic cut for this family)
- [x] 5. AwardsSite, spectacle-tolerant — eyewear studio (Aurora Optics) — DONE (`clients/_practice/aurora-optics/`), scroll-scrubbed sequence centerpiece verified with real pixel-diff test
- [x] 6. LocalSite via full menu-driven intake (Voltage Fitness) — DONE (`clients/_practice/voltage-fitness/`), type-only statement hero (new pattern), full GSAP motion layer on LocalSite tier per brief, bright/vibrant palette built from scratch

Run one at a time. Keep practice builds lean — single QA pass, not multi-round.

- [x] 7. Multi-page real build (Barba.js in a full site, not synthetic) — trades (Ironclad Multipage) — DONE (`clients/_practice/ironclad-multipage/`), 3 real pages, full nav flow verified, cursor re-init across page swaps confirmed, 2 real bugs found+fixed (missing nav links, form-guard), 0 axe/overflow/console issues across all 3 pages × 2 viewports.
- [x] 8. E-commerce content type, full creative freedom (FERUS. streetwear) — DONE (`clients/_practice/ferus-clothing/`), 3 real pages (home/shop/product), first real client-side cart in the project (localStorage, add/remove/qty/subtotal, persists correctly across Barba SPA navigation — verified end-to-end via Playwright, not just visually), 4 real bugs found+fixed (2 contrast tokens tested only against the wrong background, `aria-hidden` misuse on a focusable drawer fixed with `inert`, a heading-order skip), 0 axe/overflow/console issues across all 3 pages × 2 viewports.
- [x] 9. E-commerce content type, second build deliberately "very different" (MERIDIAN. quiet luxury) — DONE (`clients/_practice/meridian-store/`), single self-contained file from the start (applying the FERUS. multi-file lesson proactively), Higgsfield-generated Winnipeg MB video hero, drag-carousel + quick-view panel instead of a grid, real cart verified via Playwright, 2 real structural bugs found+fixed (heading-order skip, missing `<main>` landmark), 0 axe/overflow/console issues across 2 viewports.
- [x] 10. E-commerce content type, third build with different centerpiece/interaction techniques than both prior stores — first draft (FIELDWORK. technical gear) rejected wholesale by the client except the horizontal scroll-jack mechanic; rebuilt as ARLO. (ceramics/home goods) — DONE (`clients/_practice/arlo-ceramics/`, `fieldwork-store` deleted), single self-contained file, kept the scroll-jacked horizontal product rail, dropped the hotspot-pin hero, redone palette/type/layout, bottom-sheet cart kept, real cart verified via Playwright, 1 real contrast bug found+fixed (a token that passed an offline formula check by a hair failed the real browser render), 0 axe/overflow/console issues across 2 viewports.
- [x] 11. Restaurant, different register from the earlier Trattoria build (Rowan &amp; Vine wine bar) — DONE (`clients/_practice/rowan-winebar/`), single self-contained file, forest/ivory/amber wine-bar editorial palette, curated menu + wine list callout + gallery + reservation form (honeypot-protected), 2 real contrast bugs found+fixed (systemic amber-on-dark misuse across 4 call sites → led to a new "declare accent-on-light AND accent-on-dark tokens up front" standing rule; footer-bottom margin skim), 0 axe/overflow/console issues across 2 viewports.
