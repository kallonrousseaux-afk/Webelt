# Build Gotchas — bugs already paid for. Do not repay them.

Every one of these happened during a real build in this repo. Check this list before debugging.

## Animation / GSAP
1. **CSS reveal classes race GSAP `from()`** — if `.rv{opacity:0;transition:…}` is transitioning to visible when `gsap.from(...,{opacity:0})` starts, GSAP captures the mid-transition value (~0) as the END value and animates 0→0: elements stay invisible with zero errors. Fix (already in the starter): GSAP path adds `html.gsap` class; CSS `html.gsap .rv{opacity:1;transform:none;transition:none}` so GSAP owns a stable end-state. Never mix CSS transitions and GSAP tweens on the same properties.
2. **Hero line reveals need `white-space:nowrap`** on each `.ln` at desktop (normal wrap ≤900px). Without it, long lines wrap inside the overflow-hidden span, breaking the line reveal and pushing content below the fold.
3. **Pinned horizontal sections look like a giant empty block in full-page screenshots** — that's the pin runway (scroll distance), not a bug. Verify pinning live/section-by-section, not via fullPage capture.
4. **Reveal elements below the fold stay opacity:0 in screenshots** unless you scroll the page first. QA harness must walk the page (`scrollTo` in steps) before fullPage capture.

## WebGL
5. **`three.min.js` deprecated at r160+** — pin `three@0.158.0`.
6. **The full guard set** (all of it, every time): `prefers-reduced-motion` + `(hover:hover)` + `innerWidth>900` + `if(!window.THREE) return` + try/catch on renderer + DPR ≤1.5 + IntersectionObserver pause offscreen.
7. **WebGL rAF can stall screenshot tools** — capture on a fresh page load; if captures hang, screenshot section-by-section.

## Sandbox / QA environment (Claude Code cloud)
8. **CDN (jsdelivr) is BLOCKED in the sandbox browser; Google Fonts is not.** The site will silently run its no-GSAP fallback and look static. For QA: `npm install playwright three@0.158.0 gsap@3` in the scratchpad, sed the CDN URLs to `node_modules/...` in a `test-live.html` COPY (never in the real file), and test that. `npm install X --no-save` can prune other packages — install everything in one command.
9. **Playwright**: launch with `executablePath:'/opt/pw-browsers/chromium'`; do not run `playwright install`.
10. **Screenshots hide motion** — the user cannot judge an animated build from stills. Always send the actual HTML file (SendUserFile, display:render) as the preview, screenshots as backup only.

## Contrast (recurring failure)
11. **Gold/brass accents fail AA on light backgrounds** (~3.6:1) and mid-gold fails on navy (~3.9:1). Keep TWO accent tones per palette: bright (`--brass` #C9A96A-class) for dark bg text, dark (`--brass-text` #7A5E2E-class) for light bg text. Verify with:
```python
def lum(h):
    r,g,b=[int(h[i:i+2],16)/255 for i in (0,2,4)]
    f=lambda c: c/12.92 if c<=0.03928 else ((c+0.055)/1.055)**2.4
    return 0.2126*f(r)+0.7152*f(g)+0.0722*f(b)
def cr(a,b):
    l1,l2=sorted([lum(a),lum(b)],reverse=True); return (l1+0.05)/(l2+0.05)
# cr('7A5E2E','F5F1E8') must be >= 4.5 for text, >= 3.0 for large text/UI
```

## QA harness (run exactly this before "done")
```js
// scratchpad: node, playwright installed, test-live.html = copy with local libs
const { chromium } = require('playwright');
const b = await chromium.launch({ executablePath: '/opt/pw-browsers/chromium' });
// 1440x900: wait 5s (intro), screenshot; walk page in 700px steps; fullPage shot
// 375x812: same; then assert:
//   document.documentElement.scrollWidth === 375
//   document.getElementById('gl')?.classList.contains('on')  (desktop run)
//   zero pageerror events
// THEN OPEN AND LOOK AT EVERY SCREENSHOT before reporting done.
```

## Process
12. **User-declared decisions are final** — don't re-offer what was declined, don't keep flagging "pending" items on finished work.
13. **Facts are never invented** — no fake reviews/hours/credentials/stats; visible `TODO:` markers instead.
14. **Preview = the deliverable**: send the HTML file itself, mobile screenshots when asked from a phone.

## Grid overflow (found in practice build #1)
15. **Grid items overflow their track at mobile widths even with `img{width:100%}` in CSS** — a grid item's default `min-width:auto` means the browser considers the child's intrinsic/attribute size (e.g. `<img width="720">`) as a floor before percentage widths apply, blowing the whole row out past the viewport. Symptom: `scrollWidth` far exceeds the viewport at 375px even though every element "looks" responsive in the code. Fix: `.hero-grid>*,.contact-grid>*{min-width:0}` (or `min-width:0` on any grid/flex child holding a sized image). Now baked into `templates/localsite-starter/index.html` — check any OLDER copy of that file for this fix before reusing it.

## Practice build 3 findings (AwardsSite, warm-local swap)
16. **Swapping the pinned-horizontal-scroll section for a different centerpiece leaves a dangling ScrollTrigger reference** — the starter's JS has a `const track = document.querySelector('.htrack')` block wired specifically to the pinned process section. If you swap that section out (e.g. for a draggable gallery per the warm-local default), you MUST also remove/replace that JS block — it silently does nothing at runtime (querySelector returns null, then `track.scrollWidth` would throw) rather than erroring at write time. Regex/search-replace on markup alone is not enough; always grep the file afterward for the old section's class names (e.g. `.htrack`, `.hproc`) across BOTH the CSS and the `<script>` block before calling a centerpiece swap done.
17. **Bulk find-replace copy passes need a post-pass grep for leftover source-business strings** (e.g. old business name in CTA buttons, footer taglines) — string literals get reused in multiple spots (nav CTA vs footer CTA vs contact-section CTA) and a targeted replace can miss siblings with slightly different surrounding HTML. Always `grep -c` the old business name / old CTA text after the substitution pass, before QA screenshots.

## Practice build 4 findings (craft/impact register)
18. **Not every display font has an italic cut** — Archivo Expanded (used for trades/craft-impact register) has no italic variant. The starter templates lean on `font-style:italic` for accent words throughout. When swapping to a font family without italics, swap those rules to `font-style:normal;font-weight:800` (extra-bold) instead — bold-weight accents read even better in a bold/craft register than italic would have. Check the font's available styles before locking tokens in the style-tile phase, not after the copy pass.
19. **Spotlight-cursor before/after recipe (design-options-catalog) works cleanly as a centerpiece transplant** — graduated from documented recipe to verified in this build. Mobile fallback (static 50/50 clip-path split, no cursor tracking) confirmed working at 375px.

## Practice build 5 findings (spectacle-tolerant, scroll-scrubbed sequence)
20. **`scroll-behavior:smooth` (a standing project rule) breaks naive automated QA scrolling** — `window.scrollTo(x,y)` becomes an animated scroll under this CSS, not an instant jump. A test that calls `scrollTo` then checks state after a short fixed wait (e.g. 300-400ms) can sample MID-ANIMATION state and wrongly conclude a scroll-driven feature is broken. Fix: `document.documentElement.style.scrollBehavior='auto'` before any QA script does programmatic `scrollTo` checks, restore/ignore after (it's a throwaway test page). This bit the scroll-scrubbed-sequence verification in this build — the recipe was correct; the first test harness was not.
21. **Testing scroll-scrubbed/scrubbed canvas features: sample a pixel that actually changes, and use a full-canvas checksum, not a single fixed point.** A center-anchored decoration (e.g. a static marker circle) stays identical across every frame and gives a false "nothing changed" reading. Sum a strided sample across the full `getImageData` buffer instead of one coordinate.
22. **Scroll-scrubbed image sequence recipe (design-options-catalog) graduated to verified** — confirmed the pin-runway height, `ScrollTrigger scrub` progress mapping, and frame-draw logic all work correctly end-to-end (self-contained SVG data-URI frames used in place of real photography for this practice build; swap for real pre-rendered frames in production).

## Practice build 6 findings (menu-driven intake, LocalSite)
23. **Menu-driven intake confirmed working end-to-end** — AskUserQuestion flow (tier/vibe/palette/hero) plus the content-brief step, including a mid-flow user correction ("stop, redo it, let me pick") that discarded an already-QA-passed but wrongly-sourced build (a "you decide" build made when the user actually wanted the menu). Lesson: when a build is produced under the wrong intake path, don't try to salvage/relabel it — bin it and rerun the real path. Cheap to redo at this size; expensive to explain later why the "menu" build wasn't actually built from menu answers.
24. **Fonts with only one weight (e.g. Archivo Black, weight 400 only) need heading rules adjusted** — the starter's `h1,h2,h3{font-weight:600}` is a no-op/mismatch on single-weight display fonts. Same family of bug as gotcha #18 (italic-less fonts); check available weights AND styles before locking tokens, not after.
25. **Custom palettes built from scratch (not a preset) still need the contrast script run** — this build's volt-lime anchor cell used dark ink text, which happened to pass, but bright/vibrant custom palettes are the highest-risk category for accidental AA failures. Always run the gotchas contrast checker on every color pair introduced outside the documented presets.

## Retrospective fix pass (all 6 review findings acted on)
26. **Supabase RLS silently blocks every submission on a fresh table** — the #1 real-world reason these forms "just don't work" once deployed. Verified against a local mock server (success 201, RLS-block 401, honeypot no-op) — the JS logic itself has no bugs; the missing piece was documentation. Both starters now carry the required SQL directly above `SUPABASE_URL`:
```sql
alter table leads enable row level security;
create policy "anon insert leads" on leads for insert to anon with check (true);
```
Added `Prefer: return=minimal` to every fetch call across both starters and all delivered builds (cosmetic/efficiency, not a bug).
27. **Page transitions (Barba.js) recipe graduated to verified** — tested against a real 2-page setup (local npm install, CDN blocked in-sandbox): DOM swaps correctly on click, no reload, URL updates, zero errors.
28. **Floating 3D icon primitives recipe graduated to verified** — confirmed the render loop advances every frame and the scene renders correctly (screenshot-verified three lit icosahedrons); a same-pixel-sampling test gave a false negative because the sampled point sat between shapes — screenshot inspection is more reliable than single-pixel diffing for this class of recipe.
29. **WebKit/Safari hardening added to both starters** — `-webkit-backdrop-filter` prefix added alongside `backdrop-filter` (unprefixed alone silently no-ops on Safari); `min-height:100vh` declared before `100svh` as a cascade fallback for older WebKit; `color-mix()` header background given a solid `var(--surface)` fallback declared first. Verified these changes don't regress Chromium rendering (only browser engine actually testable in this sandbox).
30. **Genuine limitations that cannot be closed from this environment** — logged here rather than silently left open:
    - No real Safari/iOS device or WebKit browser engine is available in-sandbox (only Chromium via Playwright). The hardening in #29 is defensive/best-practice, not verified against real WebKit.
    - No build has been shown to an actual client or tested on a physical phone — every pass in this repo is automated (Playwright + contrast script + console errors). This is real signal but not the same as human/device judgment.
    - Sound design, Rive, and the loaded-GLB 3D model viewer recipes remain undemonstrated — each requires a real authored asset file (audio, `.riv`, `.glb`) that can't be meaningfully fabricated and verified; graduating them needs a real project that calls for one.
    - The practice queue (`clients/_practice/`) is kept as a permanent regression set (not deleted) — re-run its 6 builds after any future starter-template change to catch regressions early. See its README for how.

## Second retrospective pass
31. **Broken `<img>` placeholders in practice builds masked real QA risk** — a broken-image icon renders as a small fixed-size box regardless of the CSS intended size, which can hide layout bugs (overflow, aspect-ratio issues) that only show up once a real image loads at its real dimensions. Fixed: all 9 placeholder image references across the 3 photo-using practice builds now point to inline SVG gradient placeholders sized to match the real intended dimensions, clearly labeled "PLACEHOLDER" (not fake photography — consistent with the never-invent-facts rule). Verified 0 broken images (`naturalWidth===0` count) and 0 layout regressions across all three at 375px.
32. **design-options-catalog.md status column now accurately reflects graduation state** — draggable gallery, page transitions, floating 3D icons, scroll-scrubbed sequence, and spotlight cursor were all verified in earlier practice builds but the catalog table still said "new — see recipe below." Catalog and gotchas file were out of sync; now cross-referenced both ways. 9 of ~65 entries now marked ✅ verified (up from 5 at the first retrospective pass).

## Third retrospective pass — pushing past "needs a real asset"
33. **Loaded 3D model viewer (GLB) recipe graduated to verified** — hand-authored a minimal valid binary glTF (.glb) cube per the glTF 2.0 spec (no external tools) and loaded it with the real `GLTFLoader` from `three/examples/jsm`. Two real bugs in the TEST HARNESS were found and fixed along the way, both reusable lessons:
    - **ES modules (`type="module"`, bare specifiers like `import ... from 'three'`) require an HTTP origin — `file://` fails CORS for cross-file module imports.** Any local test of GLTFLoader/OrbitControls/addons needs a real (even trivial, single-request) local HTTP server, not a `file://` page load. Non-module `<script src>` (what every starter template actually uses) is unaffected — this only bites *test harnesses* that import Three.js addons as ES modules.
    - **Bare specifiers (`import * as THREE from "three"`) need an import map** (`<script type="importmap">{"imports":{"three":"./path/to/three.module.js"}}</script>`) — GLTFLoader.js itself imports `"three"` internally, not a relative path.
    - Confirmed via two screenshots 800ms apart that the cube visibly rotates (a different face is in view) — real render, real rotation, not just "no errors."
    - Production usage is unaffected by any of this: the starter templates load Three.js via classic `<script src>` (UMD build), which has none of these module/CORS requirements. This finding matters for future recipe testing, not for how sites actually ship.
34. **UI micro-sound (click-blip) recipe graduated to verified** — synthesized a real, tiny, playable WAV file (200ms 880Hz decay envelope, via Python's `wave` module) and confirmed `Audio.play()` resolves successfully (not just "doesn't throw") when triggered by a real user-gesture click in Playwright. Confirms the recipe's core mechanic — audio blocked without a gesture, works with one — behaves as documented.
35. **Rive remains genuinely unverifiable in this environment** — unlike WAV/GLB, `.riv` is a closed, non-trivial binary format (state machines, timelines, artboards) with no practical way to hand-author a minimal valid file without Rive's own editor or a from-scratch reimplementation of their serializer. The `rive-app/canvas` runtime can only PLAY `.riv` files, not author them. This one stays a documented-but-unverified recipe until a real project supplies a `.riv` export from the actual Rive editor.

## Fourth retrospective pass — batch graduation of pure-code recipes
36. **Morphing nav overlay, grayscale-hover, and squiggly-text recipes graduated** — all pure CSS/SVG, no external assets needed. Nav overlay: confirmed `clip-path` computed value actually changes on toggle. Grayscale hover: confirmed `filter` resolves from `grayscale(1)` to `grayscale(0)` on the hover-simulated class. Squiggly text: confirmed via before/during screenshot comparison that the SVG turbulence displacement visibly distorts the glyphs mid-animation (reading the SMIL `animVal` via JS is unreliable — screenshot comparison is the dependable check for this one).
37. **Cursor trail and percentage-counter preloader recipes graduated** — cursor trail: confirmed the trail dot's CSS transform tracks 3 real synthetic mouse-move events to their exact coordinates. Percentage preloader: generated 3 real (non-placeholder) tiny PNG files, served them over a real local HTTP server, and confirmed the load-counter fired the correct 33% → 67% → 100% sequence tied to actual `<img>` load events, not a timer.
38. **Lottie recipe graduated** — hand-authored a minimal valid Lottie JSON (one shape layer with a keyframed scale animation, per the Lottie/Bodymovin spec) and loaded it with the real `lottie-web` player. Confirmed `data_ready` fires and the shape renders at the authored color or size. Same spirit as the GLB win: a real, spec-valid asset built without external tools, not a placeholder claim.
39. **3D product configurator and instanced-3D-objects recipes graduated** — configurator: confirmed `material.color.set(hex)` on an already-rendered mesh visibly changes the rendered pixel color (screenshot-verified after tracking down two false negatives caused by test-script bugs, not recipe bugs: an async-timing race where the "before" screenshot was taken before the model finished loading, and a WebGL context-type mismatch — `getContext('webgl')` returns null on a canvas THREE already initialized as `webgl2`). Instanced objects: confirmed via Three.js's own `renderer.info.render.calls` API that 8 positioned icosahedrons render in exactly 1 draw call — the core efficiency claim of the recipe, verified with a hard number rather than a visual guess.
40. **Recurring test-harness lesson, worth its own line**: across this whole graduation effort, more false negatives came from MY test code (wrong pixel coordinates, mismatched WebGL context types, async timing races, CORS on `file://` module imports) than from actual recipe bugs. When a verification test fails, the default assumption should be "check the test" before "the recipe is broken" — confirmed by re-deriving ground truth (full screenshots, hard API numbers like draw-call counts) rather than trusting the first negative signal.
41. **Custom 404 page intentionally left without a separate test** — it's explicitly documented as reusing already-verified components (header/footer/tokens/buttons), so testing it separately would just be re-testing things already covered.

**Catalog status after this pass: 47 of ~65 entries verified** (up from 11 at the start of this session's fixing pass, up from 5 before any retrospective work). Remaining unverified: mostly WebGL/shader techniques from the Awwwards research files (stencil masking, progress-uniform transitions, three-mesh-bvh, variable-font-on-scroll — need a real variable font file) and Rive (genuinely blocked, see #35).

## Fifth retrospective pass
42. **Count-up numbers and clip-path wipe reveals graduated** — both trivial to verify directly: counter confirmed reaching its numeric target via `requestAnimationFrame` easing, clip-path confirmed transitioning from `inset(100% 0 0 0)` to a fully-revealed computed value.
43. **CSS view-timeline (`animation-timeline: view()`) — inconclusive, documented honestly rather than force-graduated.** `CSS.supports('animation-timeline: view()')` returns true in this sandbox's Chromium build, but a scroll-driven opacity reveal using it did not visibly trigger across a full scroll sweep in automated testing. Left as "pattern in refs," not verified — don't claim this one works until confirmed in a real browser session (support landed recently across engines and `animation-range` shorthand behavior may vary by version).
44. **Background video: JS discipline verified, encoding is not.** Confirmed the `IntersectionObserver` pause/play contract works correctly (paused when offscreen at load, plays once scrolled into view, pauses again when scrolled back out) using a real `<video>` element with mocked `play()`/`pause()` calls. The actual `.webm`/`.mp4` file encoding cannot be verified in this sandbox — no `ffmpeg` or any video encoder is available, and unlike GLB/Lottie/WAV, a minimal valid compressed video bitstream isn't practical to hand-author byte-by-byte. Marked ⚠️ (partial) in the catalog rather than ✅ — this is a real, permanent limitation of this specific environment, not a gap in effort.
45. **mix-blend-mode cursor and 3D hover tilt marked verified by existing production evidence** rather than re-tested from scratch — both already run correctly in the live `immigration-paralegal` build (screenshot-confirmed cursor tracking earlier in this project) and across several practice builds. Re-testing them in isolation would have been redundant; citing the existing evidence is more honest than either skipping the entry or manufacturing a duplicate test.

**Catalog status: 51 of ~65 entries verified** (up from 47). Genuinely remaining, in order of how likely they are to ever need testing for real RNR work: single-progress-uniform shader transitions, stencil-buffer masking, three-mesh-bvh (all pure WebGL/GLSL, no exotic asset blocker — just not yet tested), variable-font-on-scroll (needs a real variable font file), ambient spatial sound (needs the panner-node variant tested beyond the basic click-blip already verified), Rive (blocked — see #35), background video encoding (blocked — see #44), asymmetric grids and custom 404 (intentionally not separately tested, both reuse already-verified primitives).

## Sixth retrospective pass — the three remaining WebGL techniques
46. **Single-progress-uniform shader transitions graduated — cleanly confirmed.** Rendered the documented block-reveal shader (`step(hash(uv), uProgress)`) at progress 0 / 0.5 / 1.0 and screenshotted each: fully black at 0%, a correct checkerboard-style partial reveal at 50%, fully white at 100%. Matches the recipe exactly as written — no fixes needed.
47. **Stencil-buffer scene masking — TESTED AND FOUND BROKEN as documented. Do not ship this recipe as-is.** The awwwards-techniques.md §3 recipe interleaves raw `gl.stencilFunc`/`gl.colorMask`/`gl.stencilOp` calls between `renderer.render()` calls. In a real browser this consistently rendered the "outside mask" scene over the entire canvas — the masked cutout never appeared. Root cause: **Three.js's `WebGLRenderer` keeps its own internal state cache (`renderer.state`) and does not know about raw `gl.*` calls made outside it.** When `render()` is called again, Three may skip re-issuing GL state changes it believes are already active (e.g. `colorMask(true,...)`), because its cache still thinks that's the current state — leaving the previous pass's `colorMask(false,...)` silently in effect. Tried two standard mitigations, neither fixed it: `renderer.autoClear = false`, and `renderer.state.reset()` between passes. **Recommended fix for production use** (not yet built/verified — flagging the direction, not another false claim): don't interleave raw stencil GL calls with Three's high-level `render()` at all. Render scene A and scene B to two separate `THREE.WebGLRenderTarget`s, then composite them in a single final fullscreen-quad shader pass using the mask as a texture (`mix(colorA, colorB, maskTexture)`) — this sidesteps Three's state-cache assumptions entirely and is how most real three.js stencil/mask effects are actually implemented in production.
48. **three-mesh-bvh is version-incompatible with this project's pinned Three.js.** `npm install three-mesh-bvh` currently installs 0.9.10, whose `peerDependencies` require `three >= 0.159.0`. This project pins `three@0.158.0` (per the AwardsSite starter's WebGL rules). Loading it against the pinned version throws `The requested module 'three' does not provide an export named 'BatchedMesh'` — a hard failure, not a silent degrade. Before using this recipe on a real build: either bump the three.js pin for that specific build (re-verify the aura shader and everything else still works at the newer version first) or locate an older three-mesh-bvh release compatible with 0.158.0.

**Catalog status: 52 of ~65 entries verified.** Of the ~13 remaining: 2 are confirmed BROKEN/BLOCKED with a clear fix path documented (#47, #48) rather than silently unverified — arguably more valuable than a pass, since the next person won't waste time re-discovering the same failure. The rest (Rive, background-video encoding, variable-font-on-scroll, spatial-audio panner variant, asymmetric grids, custom 404) are either genuinely environment-blocked or intentionally not separately tested because they reuse already-verified primitives.

## Seventh retrospective pass — actually fixing (not just diagnosing) the two broken WebGL techniques
49. **Stencil masking: fixed, not just diagnosed.** Built and verified the render-target-composite replacement recommended in #47: three offscreen `WebGLRenderTarget`s (scene A, scene B, mask shape) composited in one final fullscreen-quad shader (`mix(texture2D(tA,vUv), texture2D(tB,vUv), maskValue)`). Screenshot-confirmed: a blue scene cleanly masked inside a red scene, sharp edges, zero bleed — the effect the original stencil recipe was supposed to produce but didn't. **`awwwards-techniques.md` §3 has been rewritten in place** with this corrected recipe (the broken raw-stencil version is gone, replaced, with an inline note explaining why).
50. **three-mesh-bvh: fixed, not just diagnosed.** Bisected npm versions to find the actual compatibility boundary: `three-mesh-bvh@0.8.0`'s peer-dependency claim of `three >= 0.158.0` is WRONG — every version from 0.7.x through 0.9.10 unconditionally imports `BatchedMesh`, which doesn't exist in `three@0.158.0` (confirmed via direct grep — zero matches in the installed three.module.js). **`three-mesh-bvh@0.5.24` is the fix**: zero `BatchedMesh` references, loads cleanly, and correctly raycasts a dense 1408-triangle sphere — confirmed exact hit distance (4) and intersection point (`[0,0,1]`, precisely the sphere's front surface) plus a correct miss on a ray aimed outside the mesh. Pin this exact version when using this recipe with the project's three@0.158.0; don't trust upstream peer-dependency ranges without verifying.
51. **Spatial/panner audio variant graduated** — extended the already-verified WAV+gesture test with a real `StereoPannerNode`: confirmed `AudioContext.resume()` reaches `state:"running"` (not stuck `"suspended"`, the failure mode when a gesture requirement isn't satisfied) and that `panner.pan.value` holds the assigned value, closing the gap left by the earlier UI-micro-sound-only test.

**Catalog status: 55 of ~65 entries verified.** Genuinely irreducible remainder, each with a concrete reason it cannot be closed from this sandbox:
- **Rive** — proprietary `.riv` binary format (state machines/timelines), no practical way to hand-author a minimal valid file without the actual Rive editor.
- **Background video encoding** — `.webm`/`.mp4` are compressed video bitstreams; no `ffmpeg` or any video encoder is available in this sandbox, and unlike GLB/Lottie/WAV/PNG (simple enough specs to hand-write), a valid VP8/VP9/H.264 stream is not practical to construct by hand. The surrounding JS (pause/play/fallback) IS verified — see #44.
- **Variable-font-on-scroll** — needs a real `.ttf`/`.otf` file with a `wght` variation axis; font binary formats are non-trivial to hand-author, same class of blocker as Rive.
- **Real Safari/iOS testing** — this sandbox only has Chromium.
- **A real client's reaction** — needs an actual human, not more sandbox verification.
- Two entries intentionally left untested because they only reuse already-verified primitives (asymmetric grids, custom 404).

This is the honest floor: everything with a plausible from-scratch verification path in this environment has been tried, and where a recipe was found broken, it was fixed and re-verified rather than left as a diagnosis. What remains needs a real device, a real client, a real video encoder, or a real proprietary-format asset — none of which more time in this sandbox can produce.

## Eighth pass — full regression sweep, all six builds re-verified
52. **Fix-propagation gap found and closed: a template fix doesn't retroactively reach already-delivered builds.** The RLS-policy setup comment (gotcha #26) was added to the two *starter templates* but never propagated to the six builds already copied from them before that fix landed (immigration-paralegal + all 5 practice builds). Running the actual regression sweep (not just re-reading the templates) caught this. Patched all six directly. **Lesson for future template fixes**: after patching a starter template, grep every already-delivered build for the old (unfixed) pattern and patch each one — a template fix is not retroactive.
53. **Full regression sweep run against all 6 builds (5 practice + immigration-paralegal) after every fix this session**: 375px scrollWidth clean, zero broken images, zero JS errors, RLS note present, `Prefer: return=minimal` header present, grid `min-width:0` fix present — 6/6 pass on every check.
54. **Sandbox network note**: Google Fonts requests take ~13s to fail/hang in this sandbox rather than erroring fast — `page.goto()` with default `waitUntil: 'load'` can appear to hang on a 6-page sweep. Use `waitUntil: 'domcontentloaded'` with an explicit timeout for batch QA scripts; the page still renders and is testable long before the font request resolves or times out.

## Ninth pass — real accessibility audit (axe-core), first time run all session
55. **First real automated WCAG scan of the whole session — and it found two genuine bugs the manual contrast script had missed.** Manual contrast math (the Python luminance script used throughout this project) only checks pairs you think to check. Running `axe-core` against full rendered pages caught two real "serious" violations no manual pass had:
    - **`scrollable-region-focusable`** (trattoria-awardssite) — the draggable-gallery recipe (graduated earlier this session) has no keyboard access: a `overflow-x:auto` div with no `tabindex` is mouse/touch-only, unreachable by keyboard. **Fixed in both the delivered build and the master recipe in `design-options-catalog.md`**: added `tabindex="0" role="region" aria-label="…"` to the scrollable container. This recipe is now genuinely accessible, not just visually working.
    - **`color-contrast`** (voltage-fitness) — the custom bright/vibrant palette built from scratch for that build (gotcha #25 already warned custom palettes are the highest-risk category) had `#FF3D57` failing AA at 3.17–3.46:1 in three places (nav CTA, hero magnetic button, eyebrow text) despite "passing" the manual spot-check at the time. **Fixed**: darkened to `#D91F35` (5.0:1 white-on-it, 4.57:1 text-on-bg — both clear AA), re-verified 0 violations.
56. **Lesson**: manual contrast-pair checking (this project's Python luminance script) is necessary but not sufficient — it only checks pairs you remember to test. A full-page automated scanner (`axe-core`) should run at least once per build alongside it, not instead of it. Neither approach alone would have caught both bugs found here as fast as running both together.
57. **All 6 builds re-scanned after fixes: 0 axe-core violations across the board** (up from 2 violations in 2 builds). `axe-full-results.json` captured 41-43 passing rule checks per build beyond just the violations.

## Tenth pass — real Core Web Vitals measurement, first time all session
58. **First real LCP/CLS measurement of the whole session** (via `PerformanceObserver` for `largest-contentful-paint`/`layout-shift`, not a guess). First attempt gave false 13-15 SECOND LCP readings across all 6 builds — that's the sandbox's Google Fonts host hanging (same root cause as gotcha #54), not real performance. Fixed by `page.route()`-aborting `fonts.googleapis.com`/`fonts.gstatic.com` so the blocked request fails fast instead of hanging; this isolates genuine render performance from the sandbox's specific network restriction.
59. **CLS: 0 across all 6 builds** — genuinely clean, no layout shift anywhere. This is a real pass, not a gap.
60. **LCP: 5 of 6 builds pass the ≤2.5s gate cleanly (188-708ms). One real, structural finding: `immigration-paralegal` (the actual delivered client build) measures LCP at 2564ms, just over the 2500ms gate.** Traced to the exact cause, not a mystery: the AwardsSite intro sequence (word rises .7s → holds .35s → curtain lifts .5s+.8s overlapping → hero headline lines stagger in ~1s more) totals ~2.5s by design before the largest paintable content (the hero `<h1>`) is actually visible — `.lock` on `<body>` intentionally hides everything until the sequence completes. **This is not a bug to silently patch — it's a genuine, structural tension between two of this project's own stated goals**: the cinematic AwardsSite intro (the exact thing multiple gotchas in this file fought to preserve and correctly implement) vs. the SKILL.md's own hard CWV gate. Do not shorten/gut the intro to force a pass without discussing the tradeoff with Kallon first — for a $12k+ portfolio/creative-register client, spectacle may reasonably outweigh a 64ms overage on a synthetic metric; for a client where organic search ranking matters more, the intro should be shortened or made skippable (e.g. `sessionStorage` flag to only play once per session, not on every page load) as a conscious choice, not a default.
61. **Recommendation for future AwardsSite builds with an intro sequence**: budget the intro at ≤2.3s total if the client's SEO/CWV sensitivity is unknown, to leave margin under the 2.5s gate; flag explicitly to Kallon when a build's intro pushes LCP over budget rather than letting it ship unnoticed. Real field data (Chrome UX Report, actual user devices/networks) will differ from this synthetic cold-run measurement in both directions — treat this as a warning signal to discuss, not a definitive field verdict.

## Eleventh pass — schema/JSON-LD validation, first time all session
62. **All 6 builds' JSON-LD structurally validated**: valid JSON, correct `@context` (schema.org), valid `@type` for each business's actual schema.org vocabulary (LegalService, Restaurant, Store, RoofingContractor, AccountingService, ExerciseGym — none invented), complete `address` sub-schema. TODO placeholders appear exactly where real business facts are genuinely unknown — correct behavior per the no-invented-facts rule, not a defect.
63. **Real template-level gap found: the AwardsSite starter's schema block was missing `geo`, `image`, and `openingHoursSpecification` that the LocalSite starter has always included.** Not a per-build mistake — all 4 delivered AwardsSite builds (immigration-paralegal, trattoria, aurora-optics, ironclad-roofing) consistently lacked the same 3 fields, which only happens when the *template* itself is the source. Fixed `templates/awardssite-starter/index.html`'s schema block to match the LocalSite starter's completeness, then propagated the fix to all 4 already-delivered builds (same lesson as gotcha #52: template fixes are never retroactive — check and patch every existing build). Re-validated: 0 missing recommended fields, 0 JSON errors, 0 regressions across all 6 builds' 375px/console-error checks.
64. **Schema audit conclusion: this is the third and final of the three newly-run audit categories this session (accessibility, performance, schema) — all three found genuine, real bugs, not zero findings.** Combined tally across this final audit trio: 2 WCAG violations fixed, 1 structural LCP tradeoff documented for a decision, 1 template-level schema gap fixed and propagated to 4 builds. Every audit category run this session surfaced something real — reinforces gotcha #56's point that automated scanning catches classes of bugs manual review reliably misses.

## Twelfth pass — real multi-page build with Barba.js, not a synthetic throwaway
65. **Barba.js page-transition recipe re-verified inside an actual 3-page site** (home + 2 trades service pages, matching `seo-copy-playbook.md`'s recommended multi-page structure) — the earlier graduation (gotcha #27) only tested a bare 2-page synthetic throwaway. This time: full round-trip navigation (home → page A → page B → home) confirmed real SPA behavior (URL updates, content swaps, **no actual page reload** — verified via Playwright's `load` event only firing on the true first navigation), and — new ground — **confirmed the custom cursor correctly re-binds to newly-swapped content** after a Barba transition (hover-grow + correct per-page label), which the original recipe note flagged as required (`re-run all init scripts on barba:after`) but had never actually been exercised.
66. **Real bug: building a new page from a copied template doesn't automatically wire that page's own outbound links.** `home.html`'s nav still pointed at old single-page anchors after being adapted into a multi-page site — the new service pages had correct links, but the page they were copied *from* didn't get its own nav updated. Same root cause as gotcha #17 (bulk find-replace misses siblings) but at the page-navigation level, not just copy text — a reminder that adapting a single-page starter into multi-page needs an explicit pass over every page's own nav, not just the new pages'.
67. **Real bug: a shared script block assumed every page carries the same DOM (`#lead-form`), which breaks for intentionally-leaner pages.** When a multi-page site has one full-featured hub page and several leaner supporting pages (the realistic pattern: service pages link to the homepage's contact section rather than duplicating the form), any shared script that reaches for hub-page-only elements needs a null-guard (`if (form) {...}`), not an assumption every page is identical.
68. **Another confirmed false-positive from a QA-script timing race, not a product bug** — the standard 2200ms settle-wait (used successfully on every AwardsSite page all session) was too short for these particular lean service pages, whose GSAP timeline logs harmless "target not found" warnings for later-section selectors (`.marquee-track`, `.quote`, `.cta`) that don't exist on simpler pages, apparently extending real settle time. Confirmed via direct `opacity` inspection (0 mid-scan → 1 once settled) and a longer wait resolving to 0 violations. Reinforces gotcha #40: check the test before concluding the product is broken.
69. **npm's flat `node_modules` resolution repeatedly evicted already-installed packages (three, gsap, axe-core, barba) across several separate `npm install` calls this session** — each new install could silently remove a previously-installed sibling package to satisfy the new resolution, even with `--no-save`. Fix: when a test needs multiple libraries, install them **all in one combined `npm install` command** rather than incrementally across several calls — this let the resolver settle once instead of repeatedly re-litigating the tree.

## Thirteenth pass — reduced-motion pass and a critical no-JS site failure
70. **`prefers-reduced-motion:reduce` verified clean, first real test of it all session.** Ran `immigration-paralegal` under a Playwright context with `reducedMotion:'reduce'`: the guard set (gotcha #6) correctly skips the GSAP/WebGL motion layer entirely — content is immediately visible (no intro lock, no scroll-jacking), zero console errors, 0 axe violations. No fix needed; this is the guard set working exactly as designed.
71. **CRITICAL — total site failure with JavaScript disabled, found via a real `javaScriptEnabled:false` Playwright context, not a code read.** The AwardsSite pattern's `<body class="lock">` starts locked (`overflow:hidden`, intro overlay visible) and only JS ever removes that class. With JS off, nothing removes it: the `.intro` div (`position:fixed;inset:0`, the fullscreen logo/curtain overlay) stays visible forever, `body{overflow:hidden}` never lifts. Screenshot-confirmed: the entire site renders as a permanently frozen logo screen — zero scroll, zero content, zero contact info reachable. This is a real, non-exotic failure mode (corporate/government network JS blocking, ad-blocker misconfiguration, crawler/bot rendering, accessibility tools) — not a hypothetical edge case.
   - **Fix, applied to all 8 AwardsSite-pattern files** (`templates/awardssite-starter/index.html`, `clients/immigration-paralegal/index.html`, and 6 more across `clients/_practice/`): (1) removed the static `class="lock"` from `<body>` — it's now added dynamically via `document.body.classList.add('lock')`, and only inside the `if (window.gsap && motionOK) {` branch, so a no-JS or no-motion page is never locked in the first place; (2) added `<noscript><style>.intro{display:none!important}</style></noscript>` immediately after the `.intro` div, so a no-JS page never shows the overlay at all.
   - **Re-verified**: no-JS `bodyClasses` is empty, `bodyOverflow` is `"hidden auto"` (correct default, not locked), `introDisplay` is `"none"`, hero text fully visible and readable, `scrollTo` genuinely works (scrollY reached 282 vs. stuck at 0 before the fix) — screenshot-confirmed a complete, usable, scrollable page with JS off.
   - **Regression-checked with JS/motion ON** across 4 of the 8 patched files (immigration-paralegal, aurora-optics, ironclad-roofing, trattoria-awardssite): all pass 375px overflow, 0 axe violations, 0 console errors — the fix does not affect the normal experience at all, since `motionOK` sites still add `.lock` themselves in the same place they always did.
72. **Lesson**: no build this session had been tested with JavaScript disabled until this pass, despite `<noscript>`-style graceful degradation being a stated goal in earlier passes (gotcha #10 area). A guard set that handles `prefers-reduced-motion` and missing WebGL is not the same as handling missing JS entirely — the intro-lock pattern specifically depends on JS to *unlock*, which is a strictly stronger requirement than the other guards. Any future starter-template feature that sets a blocking/locking state in static HTML must have its unlock path checked against a genuine no-JS run, not just reduced-motion.

**Catalog/process status**: all three CWV/axe/schema audits (tenth/eleventh passes) plus this reduced-motion + no-JS pass are now the standing pre-ship checklist, not one-off exercises. Genuinely remaining floor, unchanged from the ninth/tenth/eleventh passes: Rive, background-video encoding, variable-font-on-scroll, real Safari/iOS device, and real client feedback — all still blocked on missing tooling/hardware/humans, not effort.

## Fourteenth pass — social-share meta tags, another template-level gap
73. **Real gap: the AwardsSite starter had no `og:image` at all, and neither starter had Twitter Card tags.** Found by grepping for `og:image`/`twitter:` across both starters — LocalSite had `og:image` but AwardsSite never did, and Twitter's own card protocol (`twitter:card`/`twitter:title`/`twitter:description`/`twitter:image`) was missing from both, meaning a link shared on Twitter/X would render with no preview image or text at all (Twitter does not reliably fall back to bare OG tags). Same class of bug as gotcha #63 (schema fields missing from one starter but not the other) — a template-level inconsistency, not a per-build mistake.
    - Fixed both starters: added `og:image` to the AwardsSite starter (TODO placeholder path, matching its existing TODO-DOMAIN pattern), added the 4 Twitter Card tags to both.
    - **Propagated to all 9 already-delivered builds** that predate this fix (immigration-paralegal + 8 practice builds, including all 3 multi-page files) — same lesson as gotchas #52/#63: a template fix is never retroactive. Verified via grep (`twitter:card` count = 1 in all 11 total files including the 2 starters) and an HTML parse sanity check (no tag corruption from the scripted insert).

## Fifteenth pass — real browser back/forward through Barba, found 3 real bugs
74. **CRITICAL — a committed build referenced `node_modules/` as a live script `src`, not a CDN URL.** All 3 files in `clients/_practice/ironclad-multipage/` had `<script src="node_modules/@barba/core/dist/barba.umd.js">` baked into the committed HTML — a leftover from an earlier local-testing sed swap that was never reverted before commit. In any real deployment (or even opening the file outside this exact test setup) this 404s and Barba never loads: motion never activates, `body.lock` never lifts (same failure class as gotcha #71, different cause), the site is stuck locked. **Fix**: replaced with `https://cdn.jsdelivr.net/npm/@barba/core@2/dist/barba.umd.js`, matching the CDN pattern already used for the adjacent gsap/ScrollTrigger tags on the same lines. **Lesson**: after using a `node_modules`-swapped copy for local QA (the standard pattern in gotcha #8), always diff against the real committed file before finishing — never let a test-only path leak into the file that gets shipped. A `grep -rl node_modules clients/ templates/` sweep should be part of the pre-commit routine going forward, not just a one-time catch.
75. **Real bug, found via an actual Playwright `goBack()`/`goForward()` test (never run before this pass): Barba swaps `[data-barba="container"]` but never touches `<head>`, so `document.title` silently went stale on every internal navigation.** Clicking from home → storm-damage-repair.html correctly swapped the URL and body content but left the browser tab title reading "Ironclad Roofing" (the homepage's title) instead of the visited page's — a real, user-visible + SEO-relevant defect (tab title is a ranking/UX signal Google and users both see) that no prior QA pass in this project had ever exercised, because every earlier check only confirmed DOM/URL swap, not `<head>` state. **Fix, applied to all 3 pages' `enter` hooks**: extract the new title from Barba's `next.html` (the raw fetched HTML string, confirmed present via `Object.keys(next)` — `[container, html, namespace, url]`) with a simple regex and assign to `document.title`. Verified end-to-end: title correctly updates on click, correctly reverts on browser Back, correctly re-updates on browser Forward, with `navCount` staying at 1 throughout (proving no real page reload occurred at any step — genuine SPA behavior, not a hidden full-navigation fallback).
76. **A second, larger bug the title-sync fix exposed**: `storm-damage-repair.html` and `roof-replacement.html` had **completely duplicate `<title>`, meta description, canonical URL, and og/twitter title/description** — byte-identical to `home.html`'s, copy-pasted during the single-page→multi-page adaptation (gotcha #66's root cause, recurring in the metadata instead of the nav) and never caught because no prior pass diffed `<head>` content across sibling pages. Three pages sharing one canonical/title/description is a real, serious SEO defect (search engines may only index one of the three, or treat the duplicates as unintentional cloaking signal). **Fixed**: gave each service page its own accurate title/description/canonical/OG/Twitter values, sourced directly from that page's actual `<h1>` content (e.g. `roof-replacement.html`'s h1 "A full roof replacement, done right the first time" → title "Roof Replacement in Winnipeg | Ironclad Roofing"), not invented copy.
77. **Process lesson for future multi-page builds**: adapting a single-page starter into multiple pages needs an explicit `<head>`-metadata pass (title/description/canonical/OG/Twitter, one set per page) as its own checklist item, separate from the nav-links pass (#66) and the shared-script-DOM-assumption pass (#67) — three different bug classes were found in three different passes from the same single root cause ("copy a page, forget to make every page-scoped thing actually page-scoped"), and grepping for one doesn't catch the others.

## Sixteenth pass — robots.txt, sitemap.xml, print stylesheet: none of these existed anywhere
78. **Real, previously-total gap: no build in this project — not the starters, not any delivered client/practice build — has ever shipped a `robots.txt` or `sitemap.xml`, and neither is mentioned anywhere in `SKILL.md` or any reference file.** These are basic, standard production requirements (crawl directives, indexable-page discovery) that simply never made it onto the checklist. Fixed: added `templates/robots.txt` and `templates/sitemap.xml` as copyable masters (single root `<url>` entry, commented example for extra pages), added a step to `SKILL.md`'s Step 5 (Deliver) requiring both be copied and domain-swapped for every real build, and propagated actual `robots.txt`/`sitemap.xml` files (with each build's real canonical domain where known) to all 7 already-delivered single/multi-page builds — the multi-page build's `sitemap.xml` correctly lists all 3 real pages, not just the root.
79. **Real gap: neither starter had a `@media print` rule.** A site with zero print styles prints its full dark-background WebGL-canvas/cursor-dot/intro-overlay chrome verbatim — wastes ink, unreadable, and hides real content (e.g. a service page someone tries to print for reference) behind decorative layers never meant to be static. Fixed both starters with a minimal print block: hides cursor/WebGL/video/intro/orbit/scroll-cue decoration, forces white background + dark text + underlined links, forces all `.rv` reveal elements permanently visible (no relying on JS/scroll state that print never triggers), and adds `page-break-inside:avoid` on hero/section blocks. Propagated to all 9 already-delivered builds (matching the AwardsSite vs. LocalSite variant to each). Verified via HTML parse + exact-one-instance grep across all 11 files (2 starters + 9 builds) — zero corruption from the scripted insert, and a Playwright smoke check confirmed the change is purely additive (no screen-layout impact, `scrollWidth` unchanged).
80. **Lesson, same shape as #52/#63/#73**: every one of these gaps (RLS docs, schema completeness, social meta tags, robots/sitemap, print styles) was a *template-level* omission, not a per-build mistake — meaning it silently affected every single build made before the fix. The recurring fix pattern is now well-established: (1) fix both starters, (2) grep every already-delivered build for the old/missing pattern, (3) patch each one directly, (4) verify with a parse/count check, not just a visual skim. Worth checking as a standing habit before believing "the skill is done": does this class of file/tag exist ANYWHERE in the project, or does it just feel like it should by now?

## Seventeenth pass — lead-form XSS/injection check, first security-focused test all session
81. **Checked both starters' lead-capture forms for reflected XSS — clean, no fix needed.** Every place submitted form data touches the DOM (`msg.textContent = '…'`) uses `textContent`, never `innerHTML`; the raw field values only ever leave the page as a `JSON.stringify()` request body to Supabase, never re-rendered. Confirmed with a real Playwright test: submitted `<img src=x onerror=alert(1)>` as the name field and `<script>window.__pwned=true</script>` as the message field through an actual form-submit event — `window.__pwned` stayed `undefined`, confirming no injected script executed. This guarantee holds structurally (DOM API semantics, not a runtime coincidence) as long as future edits keep using `textContent`/`value` and never introduce `innerHTML`/`insertAdjacentHTML` with user-supplied data — worth a specific check if the form-handling script is ever rewritten.
82. **Scope note**: this only covers client-side reflected XSS on the page itself. Stored-XSS risk (a lead's name/message rendered unsafely in a future admin dashboard that reads the Supabase `leads` table) is out of scope of a static HTML build and depends entirely on how that future consumer renders the data — flag this to Kallon if/when an admin view of leads is ever built.

## Eighteenth pass — self-caused bug: the print-CSS insert from pass 16 broke print for everyone except reduced-motion users
83. **CRITICAL, self-inflicted: the scripted `@media print` insert in the Sixteenth pass (#79) never actually closed the preceding `@media(prefers-reduced-motion:reduce){…}` block — the print rule landed NESTED inside it instead of as a sibling.** Nested media queries require BOTH conditions to match, so the print stylesheet only ever applied to visitors who print AND have reduced-motion enabled — i.e., almost nobody. Everyone else printing a page got the exact unfixed, unreadable, full-dark-chrome output #79 was written to prevent. This is a direct consequence of the insertion script using a naive `"  }\n</style>"` string match: the reduced-motion block in the AwardsSite/LocalSite starters (at the time) happened to be the last rule before `</style>`, and the script assumed that final `}` belonged to it — but on files where an intermediate edit had already added something after it, the match could land wrong. Found by manually reading the file for an unrelated reason (starting a new build) and noticing the indentation/brace structure looked off, not by a targeted test — the earlier pass's own verification (grep count + HTML parse) checked for *presence* of the block but never checked *CSS nesting correctness*, so it passed a broken insert.
    - **Fixed**: inserted the missing `}` to properly close the reduced-motion block before `@media print` in all 11 affected files (2 starters + 9 builds). Verified with a brace-depth counter across every file's `<style>` block (all now balance to 0, none go negative) AND a real Playwright check emulating `media:'print'` on a normal (non-reduced-motion) context — confirmed `#gl` (the WebGL canvas) now correctly gets `display:none` under print for an ordinary visitor, not just a reduced-motion one.
84. **Lesson: a scripted multi-file insert needs a structural check, not just a presence check.** Grep-for-the-tag and HTML-parse-succeeds (the verification used in pass 16) both stayed green while the CSS was semantically broken, because malformed nesting is still syntactically valid CSS — it just means something different. For any future scripted CSS insertion across multiple files, add a brace-balance / nesting-depth check as part of verification, not just "does the string exist" and "does the file still parse as HTML."

## Nineteenth pass — alternate immigration/paralegal build (v2), a real second AwardsSite direction on the same content
85. **Built `clients/immigration-paralegal-v2/` — same business facts as v1, deliberately different design: light editorial paper/clay palette (vs. v1's dark navy/brass), Fraunces (vs. Playfair Display), type-only hero with no WebGL (vs. the aura shader), and a scroll-scrubbed "document, stamped through" sequence as the one centerpiece (vs. v1's aura + pinned-horizontal-cards + continuity orbit).** Confirms the transplant workflow supports genuinely different AwardsSite directions on identical content, not just token swaps on the same skeleton.
86. **Fullpage-screenshot artifact, not a bug: a `position:fixed` header appeared duplicated mid-page in a Playwright `fullPage:true` capture.** Looked like a real overlap bug at first glance (nav bar rendering on top of hero copy partway down the page). Verified it was a screenshot-capture-only artifact, not live behavior, by (1) checking the header's actual computed `getBoundingClientRect()` (`top:0, bottom:94.8`, correctly pinned) and (2) taking a normal (non-fullPage) viewport screenshot, which showed the hero rendering perfectly clean. Same family as gotcha #3 (pinned sections look wrong in fullPage captures) — extend that lesson explicitly to `position:fixed` headers, not just `position:sticky`/pinned-scroll sections: verify any fixed/pinned element's real behavior via `getBoundingClientRect()` or a normal-viewport screenshot before treating a fullPage capture as ground truth.
87. **Real contrast fail caught by the standard checker, fixed before delivery**: this build's custom clay accent (`#B5551F`) measured 4.43:1 on the paper background — under the 4.5:1 AA text threshold — and was used for small non-large text (`.nap a.tel`, focus-state labels), not just large headline accents where 3.0:1 would've been enough. Darkened to `#A64E1C` (5.09:1 on paper, 5.63:1 for white CTA text on it) and replaced all 7 occurrences (the CSS var plus 6 literal hex values baked into inline SVG data-URIs for the favicon and scroll-scrubbed sequence frames — a scripted color swap needs to catch literal hex duplicates, not just the CSS custom property). Re-verified 0 axe violations after the fix. Reinforces gotcha #25: every custom palette, however carefully chosen, needs the contrast script run on every pair before delivery.

## Twentieth pass — v2 went through two more real revisions on direct client feedback
88. **Real feedback loop, not a one-shot build**: after seeing draft 1 (clay/navy palette, scroll-scrubbed square canvas centerpiece), Kallon rejected the centerpiece outright ("that square is gross, don't use that again") and asked for new color combos. Draft 2 added a second accent (`--forest`) and replaced the canvas with a horizontal scroll-drawn line timeline (SVG `stroke-dashoffset` scrub) — genuinely different technique, no canvas. Kallon then rejected the timeline too and asked for a *completely* new palette, not just added accents. **Draft 3**: fresh palette (warm ivory paper / deep plum dark sections / muted teal accent, sharing no hex values with drafts 1-2) and a third distinct centerpiece — a pinned stacked-panel reveal using `clip-path` wipes. Lesson: when a client rejects a specific element twice in different forms (square canvas → line timeline, both centerpiece attempts), the fix isn't a variation on the same idea — pick a technique from a genuinely different family (pinning + clip-path stacking, not scroll-scrubbed imagery of any kind).
89. **Real bug found in the new centerpiece during its own build**: `.stack-panel` elements were `position:absolute;inset:0` with no `z-index` and no explicit background, so during the `clip-path` wipe transition both the outgoing and incoming panel's text rendered simultaneously at the same screen position — genuinely overlapping, unreadable text (screenshot-confirmed: "Filing & Preparation follow-through" and two full paragraphs stacked on top of each other). Fixed by giving each panel an explicit ascending `z-index` (later panels stack on top) and an opaque `background:var(--plum)` so each panel fully occludes whatever is beneath it as its clip-path opens. Any future "stacked absolutely-positioned panels revealed via clip-path" recipe needs both `z-index` ordering and an opaque background baked in from the start — this is a structural requirement of the technique, not a one-off mistake.
90. **Declining a technique for one client doesn't retroactively deprecate it project-wide** — the scroll-scrubbed-sequence recipe (design-options-catalog.md) is still verified and still fine for content that's actually shaped like a rotation/before-after view (it's used correctly in `aurora-optics`). Added a note directly on that catalog entry flagging Kallon's specific rejection for this register/client, rather than deleting or downgrading the recipe's overall verified status — the distinction between "declined for this build" and "broken/unverified" matters and shouldn't be conflated.

## Twenty-first pass — a structural CSS bug that silently broke both correctness AND accessibility state, caught by the client, not by automated QA
91. **CRITICAL class of bug: `:first-child`/`:nth-child()` selectors silently fail when a non-matching sibling (like a decorative `.stack-dots` div) sits earlier in the DOM than intended.** In the v2 pinned-panel-stack centerpiece, `.stack-panel:first-child{clip-path:inset(0 0 0 0)}` was meant to keep panel 1 visible by default — but `.stack-dots` (the progress-indicator div) was the *actual* first child of the pinned container, so the selector matched nothing. Panel 1 silently inherited the general `clip-path:inset(100% 0 0 0)` (hidden) rule instead, meaning the very first thing a visitor saw on entering the pinned section was **panel 2**, not panel 1 — a real, user-visible defect that every automated QA check in this project's harness (axe, overflow, console errors, brace balance, HTML parse) passed cleanly on, because none of them assert "is the FIRST logical step of a 4-step sequence actually the first thing shown." **Kallon caught this by eye, not any test in this repo.** The same root cause simultaneously broke the panel `z-index` stacking (`:nth-child()` rules were all off by one, and the last panel got no `z-index` rule at all).
92. **Fix and lesson**: replaced both structural pseudo-selectors with the DOM's own `data-panel` attribute (already present, added for exactly this kind of robustness) and an explicit `.first` class — selectors that don't silently break if an unrelated sibling shifts position. **`:first-child`/`:last-child`/`:nth-child()` are fragile the moment a container has ANY non-uniform children** (icons, indicator dots, decorative wrappers) mixed in with the "real" repeated items — prefer an explicit class or data attribute for "which one is first/last/Nth" whenever the repeated items aren't guaranteed to be the container's only children. This is a durable pattern to watch for in any future centerpiece using positional CSS selectors on a mixed-content container.
93. **A second class of bug from the same build, also client-caught, not QA-caught**: the pinned timeline's GSAP `.to()` transitions were scheduled back-to-back with zero hold time (`i - 1` positions, duration 1 each) — meaning the wipe to panel 2 began the instant the section pinned, with no time to actually read panel 1. Automated QA had no way to flag this either (nothing renders incorrectly, no accessibility violation, no overflow) — it's a pacing/UX judgment call, exactly the kind of thing that needs a human watching the real animation, not a scripted assertion. Fixed with an explicit hold+transition timeline structure (each panel gets a real scroll-distance hold before wiping). **Standing lesson for this whole session**: automated QA (axe/overflow/console/parse/contrast) catches a specific, valuable, but bounded class of bugs — it does not catch "does this animation read correctly to a human," "is the pacing right," or "did we accidentally skip the first step." Kallon's eyes-on review after each delivered build is not a redundant final step; it is catching a category of real bug no test in this file's arsenal is built to find.

## Twenty-second pass — v3 draft B: sidebar rejected, added a real WebGL centerpiece
94. **A build with genuine motion (intro reveal, cursor, magnetic CTAs, scroll reveals, hover/click interactions) still reads as "no animations at all" to a client if it lacks a scroll-driven centerpiece moment.** v3 draft A deliberately avoided the marquee and any pinned/scroll-scrub technique per earlier feedback — the result technically had motion, but none of it was the kind of "big moment" spectacle v1 (aura shader) and v2 (pinned panel stack) both had. **Lesson**: "no marquee, no slide" is not the same instruction as "less motion overall" — an AwardsSite build still needs its one centerpiece (per `technique-selection.md` Filter 3), just not a repeat of a previously-declined one. When in doubt about how much motion reads as "enough," ask rather than default toward restraint — under-delivering on an AwardsSite build's signature spectacle is exactly the failure mode `technique-selection.md` already warns about (Filter 1: "never under-deliver AwardsSite either").
95. **New centerpiece technique, genuinely distinct from both prior drafts, graduated to verified in production use**: five lit Three.js primitive meshes (icosahedron, torus, octahedron, box, cone — one per service pathway), each independently rotating via `requestAnimationFrame`, plus a GSAP `ScrollTrigger`-driven group rotation as the visitor scrolls past the hero. Confirmed via `#gl3d.classList.contains('on')` that the scene actually initialized and rendered (not just attempted) — same verification pattern as v1's aura shader. Full WebGL guard set applied identically to every other WebGL centerpiece in this project (gotcha #6).
96. **Real layout bug found in this new centerpiece before delivery**: the WebGL canvas was sized `width:56%` positioned at the hero's right edge, but the hero copy column (max-width 640px) extends past the 44% mark on a standard 1440px viewport — the canvas region and the text region overlapped, and the objects' orbital spread (radius 2.6) pushed shapes even further left into the headline. Screenshot-caught immediately (dark geometry visibly obscuring "in Canada, filed right the first time"). Fixed by narrowing the canvas to `38%` width and tightening the orbital radius to 1.5 — re-verified clean with zero text overlap. **Lesson, same shape as gotcha #56/#68 etc.**: a percentage-width absolutely-positioned decorative layer needs to be checked against the ACTUAL rendered width of the content it's supposed to avoid, not just eyeballed as "should be fine" — measure or screenshot-verify the real overlap, especially when both the decorative layer's size and the text's length can vary.

## Twenty-third pass — real bug in the 3D centerpiece's own scene sizing, and a first real generation-tool workflow
97. **Client-caught bug, not QA-caught: the WebGL centerpiece's objects looked visibly squished (ovals instead of a circular torus, a stretched cone).** Root cause: `resize()` computed `renderer.setSize`/`camera.aspect` from `hero.clientWidth`/`clientHeight` — the FULL hero element — while the canvas itself was CSS-constrained to `width:38%`. The internal render buffer's aspect ratio (full-hero-width : hero-height) didn't match the canvas's actual displayed aspect ratio (38%-width : hero-height) that the browser then squeezed it into, distorting every object. **Fixed** by sizing off `canvas.clientWidth`/`canvas.clientHeight` instead — confirmed via a direct measurement (`canvas.clientWidth/clientHeight` vs `canvas.width/height` buffer dims) that the aspect ratios now match exactly (0.608 both), and a screenshot confirming the torus is now a true circle. **Lesson**: whenever a WebGL canvas is styled to a *different* aspect ratio than its containing element via CSS (percentage width, `object-fit`, etc.), the resize handler must measure the canvas's own rendered dimensions, not a parent's — this is a distinct failure mode from the #96 overlap bug (that was position/spread; this was aspect-ratio mismatch), both caught in the same centerpiece, both by the client rather than by axe/overflow/console checks (neither produces a DOM-level error — the render just looks wrong to a human eye).
98. **New standing rule added to `SKILL.md`**: ask before choosing between hand-authored primitive geometry and imported real 3D models for any 3D-based technique — mirrors the pre-existing "image generation always asks first" rule. Direct instruction from Kallon after seeing the primitive-shape centerpiece: "before you do 3d models on a s[i]ght ask me if I want to import some."
99. **First real Higgsfield video-generation workflow used end-to-end in this project.** Generated a real 5s 1080p video (`kling3_0_turbo`) after explicit two-part confirmation (concept: "you decide" vs. a specific idea; target build) via `AskUserQuestion`, matching the project's ask-before-generating pattern. The job took ~16 minutes to complete (unusually slow — checked `balance`/`transactions` mid-wait to rule out a stuck/failed job before assuming something was broken; credits were charged correctly and the job was still legitimately `in_progress` server-side, not stuck) — don't assume a slow generation job is broken; check balance/transaction state before concluding something needs fixing, and keep the user informed if a wait stretches past a few minutes rather than polling silently.
100. **Real environment limitation, not a bug: the sandbox's outbound proxy blocks the Higgsfield/CloudFront CDN host (`d8j0ntlcm91z4.cloudfront.net`), returning 403 on the CONNECT tunnel.** Per `/root/.ccr/README.md`, a 403/407 from the proxy means the destination isn't on the org's egress allowlist — explicitly NOT something to retry or route around. This means a generated video/image asset cannot be downloaded and re-hosted locally from within this sandbox. **Working pattern**: reference the generation CDN URL directly in the `<source>`/`<img>` tag — this works fine once the file is opened in a real browser outside the sandbox's network policy — and leave an explicit `TODO:` comment (plus a README note) instructing the human to download and re-host the asset on the real domain before production launch, since a third-party generation CDN link is not a permanent production-safe dependency.
101. **Verified the video `<video>` element degrades gracefully when its network fetch is blocked (i.e., exactly this sandbox's own condition) — genuinely useful signal, not just a workaround.** With the CDN blocked, the video never fires `loadeddata`, so `#heroVideo` never gets its `.on` class and stays `opacity:0` behind the static paper-colored veil — zero broken layout, zero console error, zero visual glitch. This incidentally confirms the recipe's failure mode is safe for real-world slow/failed video loads too (e.g. a mobile connection that stalls mid-fetch), not just the sandbox's artificial block.

## Twenty-fourth pass — first real e-commerce build (FERUS. streetwear, 3 pages, full creative freedom), 4 real bugs
102. **First genuinely different content type this project has built: a product catalog + real client-side shopping cart, not a lead-gen service business.** `localStorage`-backed cart (add/remove/quantity/subtotal), persisting correctly across all 3 real pages through Barba SPA navigation — verified end-to-end via a real Playwright flow, not a visual check: added a product on `shop.html`, incremented quantity via the drawer's `+` button, clicked a real nav link to `product.html` (confirmed via `page.url()` that Barba changed the URL with no reload), confirmed the cart count and full `localStorage` state survived, then added the product-detail page's item and confirmed it correctly *merged into the existing line item* (same id+size → quantity increment) rather than creating a duplicate entry. This is the first build in the whole project with genuinely stateful, mutating client-side application logic beyond a one-shot lead form.
103. **`aria-hidden="true"` on a container that still holds focusable children is itself a WCAG violation, independent of whether it's visually off-screen.** The cart drawer's static markup had `aria-hidden="true"` from load, containing a close button, checkout button, and (once populated) quantity +/− controls — axe's `aria-hidden-focus` rule correctly flagged this even though the drawer was transformed off-screen (`translateX(100%)`) and visually invisible; a sighted keyboard user could still Tab into "hidden" content, and `aria-hidden` on focusable descendants is an explicit WCAG failure mode, not a style problem. **Fixed by switching to the `inert` attribute** (toggled: set when closed, removed when opened) instead of `aria-hidden` — `inert` correctly disables both focusability AND AT-exposure in one attribute, which is the actual semantic needed for a "currently closed, will become interactive" panel. This is a durable pattern for any future off-canvas panel/drawer/modal recipe in this project: use `inert`, not `aria-hidden`, whenever the hidden content contains interactive elements.
104. **Two separate contrast-token bugs, both from a color being used against a background it was never actually checked against.** `--muted-dark` was named/intended for dark-background use but measured only 2.94:1 there (fixed: `#5C5C56` → `#85857C`, 5.32:1). `--acid-deep` was correctly contrast-safe everywhere it had been used in prior builds (dark backgrounds) but this build introduced its first-ever use on a *light* section background and nobody re-checked it there — 1.65:1, a severe fail (fixed: `#9FCC1F` → `#4C610E`, 6.09:1 on the light bg, scoped to that specific token since it's genuinely only used there). **Lesson, worth stating plainly**: a color token being "already verified safe" only means safe against the specific background(s) it was checked against at the time — reusing an existing token against a NEW background pairing (even one from the same design system) needs its own fresh contrast check, not an assumption of inherited safety.
105. **Confirmed, not assumed, that this specific `--acid-deep` failure was a real bug and not a QA-script timing race**: an isolated no-scroll axe run showed 0 violations (the `.news` section hadn't scrolled into view, so its GSAP reveal hadn't fired, so axe had nothing visible to measure — same shape as gotcha #4/#9's "below-the-fold reveals stay invisible in checks that don't scroll"), while the standard scroll-walk harness caught it. The distinguishing test: pulled the SPECIFIC failing node/contrast-ratio number from both runs rather than just comparing pass/fail counts — a real bug reproduces with the same exact failing selector and number every time you scroll it into view; a genuine timing race would show inconsistent/different transient states across repeated runs. Confirmed 1.65:1 on the same `.eyebrow`/`em` elements consistently.
106. **Heading-order violation from repeated card components (`<h3>` grid items) appearing directly after a page's `<h1>` with no `<h2>` between them** — same underlying pattern as gotcha #91/#92 (a template being copy-pasted into a new page layout without re-checking structural assumptions), but this time a semantic HTML/accessibility issue rather than a CSS selector bug. Fixed with a visually-hidden (`.sr-only`) `<h2>Products</h2>` immediately before the grid — invisible to sighted users, restores correct heading hierarchy for assistive tech and SEO crawlers. Added a reusable `.sr-only` utility class to the shared style block for future use.
107. **CRITICAL: a multi-file build (3 HTML pages + a shared external `<script src="ferus-shared.js">`) breaks completely when only the HTML files are downloaded/shared individually — the intro overlay never resolves, because ALL the JS that would hide it lives in the file that never came along.** Delivered the 3 HTML files via `SendUserFile` without the shared JS file; the user opened one, saw only the stuck "Ferus." intro word and nothing else — a real, total site failure, structurally identical in symptom to gotcha #71 (the no-JS lock bug) but with a completely different root cause (here JS is fully enabled, one specific file 404s because it wasn't in the same folder as the HTML). **This is a delivery-format bug, not a code bug** — the multi-page site itself is correct and works fine when the files are actually served together (confirmed earlier via a local HTTP server in QA). The failure only appears in the "download a single file and open it" distribution path, which is exactly how a preview link/download typically gets used.
108. **Fix: built a genuinely separate single-file artifact (`onepage.html`) with all "pages" as anchor-linked sections on one document and all JS inlined**, rather than trying to make the multi-page files somehow self-contained (they can't be — that's what makes them multi-page). Reused the exact same CSS/product-data/cart logic, adapted only the navigation model (anchor scroll instead of Barba) and the product-detail lookup (click-to-swap-in-place instead of a `?id=` query param, since there's only one URL now). **Verified independently** — brace-balance + HTML parse + a fresh Playwright run confirmed the intro now resolves (`introDisplay:'none'`), both grids populate, and a full add-to-cart → quantity → click-a-different-product → add-again flow correctly produces two distinct line items in `localStorage`.
109. **A copy-paste snapshot of the shared `<style>` block, taken before 2 earlier bug fixes landed, silently reintroduced both already-fixed bugs (`--muted-dark` and `--acid-deep` contrast fails) into the new single-file build**, plus omitted the `.sr-only` class definition entirely (used in markup, never defined — a silent no-op, not an error). The style block had been captured to a scratch file (`/tmp/ferus-style.txt`) once, early, and reused to scaffold `onepage.html` without re-capturing after the later fixes. **Lesson, same shape as gotcha #52/#63/#73/#80 but at scratch-file scope instead of template-file scope**: any reusable snapshot (scratch file, clipboard, copy-pasted block) taken from a file that gets fixed LATER needs to be re-taken, or the fix needs to be re-applied to every place the snapshot was already used — a stale local copy is exactly as dangerous as an un-propagated template fix, just easier to miss because it isn't tracked in git.

## Twenty-fifth pass — second e-commerce build (MERIDIAN.), single-file from the start, applying the FERUS. lesson directly
110. **Applying gotcha #107/108's lesson proactively, not reactively**: built MERIDIAN. as a single self-contained `index.html` from the very first draft — anchor-linked sections instead of separate pages, all JS in one inline `<script>` — rather than building multi-file first and fixing it after a broken-download report. Zero download-failure risk by construction, confirmed by the fact that no "shared file" ever existed to go missing.
111. **Second real Higgsfield video-generation job in this project ran fast (~2 minutes) vs. the first job's ~16 minutes** — confirms job duration is genuinely variable per-job, not a fixed property of the model/params (same `kling3_0_turbo`, same 5s/1080p/16:9 settings as the slow job). Reinforces gotcha #99's lesson: don't assume a specific wait time; poll patiently and check `balance`/`transactions` only if a job seems stuck, not on a fixed schedule.
112. **`@axe-core/playwright`'s `AxeBuilder` requires a `BrowserContext`-derived `Page` (via `browser.newContext()` → `context.newPage()`), not a bare `browser.newPage()`** — calling `.analyze()` on a page created directly from `browser.newPage()` throws `Error: Please use browser.newContext()` immediately, before any scan runs. Easy to miss because `browser.newPage()` is valid Playwright API on its own (implicitly creates a throwaway context) — it's specifically the axe integration that needs the context handle explicitly in scope. Fixed by always creating `const context = await browser.newContext(...)` first in this project's QA harness pattern going forward.
113. **Two real bugs found by axe on first pass, both structural (not visual/CSS)**: (a) footer used `<h4>` immediately after the page's last `<h2>` (news section), skipping `<h3>` — same shape as gotcha #106's heading-order pattern; fixed by making the footer's column headings `<h3>`. (b) `region` violation (29 nodes) — the entire page body sat directly under `<body>` with no `<main>` landmark wrapping it between `<header>` and `<footer>`; fixed by wrapping the hero/statement/shop/feature sections in `<main>…</main>`. Both are new structural patterns to check for on any future single-file build that skips a boilerplate/template scaffold (this build was authored fresh, not derived from an existing template that already had `<main>`/correct heading levels baked in).
114. **A sed-based multi-step tag rename (`<h4>`→`<h3>` in two spots, applied via chained `sed` substitutions matching first-occurrence-only) produced a mismatched tag pair (`<h3>Shop/h4>`) that a naive text scan would miss** — caught immediately by the next axe re-run (still flagged as broken/0-heading structure) and a manual `grep -n` sweep of all heading tags in the file, which is the reliable way to verify a bulk heading-level edit landed correctly: grep every `<h[1-6]>`/`</h[1-6]>` occurrence and read the sequence by eye, don't trust that a scripted replace closed correctly.
115. **Confirmed real-world graceful-degradation validity extends to a second, differently-composed hero**: same `IntersectionObserver` pause/play + opacity-based `.on` class pattern from gotcha #101 verified again on a completely different layout (bottom-aligned content, gradient veil, header color-flip tied to hero visibility) — the pattern generalizes cleanly across different hero compositions, not just the one it was first built for.
116. **Client feedback ("colors on pages when switching are harsh") led to a soft scroll-linked background crossfade between sections — first attempt was a real accessibility regression, second attempt fixed it architecturally.** First attempt: removed each section's own `background-color`, added one `position:fixed` backdrop `<div>` behind everything, and had GSAP paint the backdrop's color as the user scrolled past each section boundary. Two real problems, not just one: (a) axe (and any DOM-ancestor-walking contrast checker, which is how contrast is actually computed for a11y purposes) can't see a `position:fixed` SIBLING element as a text node's effective background — it isn't in the ancestor chain — so it fell back to reporting white (the true default with no ancestor background at all) and flagged every dark-section text node as a serious contrast failure; this is not a false positive, the DOM genuinely has no provable background for that text anymore. (b) The deeper bug: since the backdrop's color was 100% JS-painted with no CSS fallback, if GSAP fails to load (blocked CDN — literally this project's own sandbox condition per gotcha #100, but equally true for any real visitor with JS disabled or a failed script load), every "colored" section silently falls through to plain white instead of its intended color — a real visual/functional regression, not a tooling artifact. **Fixed** by keeping each section's own real CSS `background-color` as declared (so the zero-JS/zero-GSAP state is pixel-identical to the original hard-cut design and independently passes contrast checks), and having GSAP `fromTo`-tween that section's OWN background-color property (reading its CSS-declared target via `getComputedStyle` rather than hardcoding it twice) instead of a decoupled backdrop layer. **Lesson, worth generalizing**: any visual effect built by moving a background/color declaration OFF of the actual content's ancestor chain and onto a separate synced layer needs to be checked against both (a) DOM-ancestor-based contrast tooling, which won't traverse sideways to a sibling no matter how convincingly it's positioned, and (b) the true no-JS fallback state, not just "does it look right when the enhancement script actually runs." **Standing pattern going forward**: any section-to-section color transition in this project should default to the "tween the section's own background-color, CSS-declared fallback first" approach — never a separate synced overlay/backdrop layer.

## Twenty-sixth pass — third e-commerce build (FIELDWORK.), third distinct centerpiece/interaction set, a real string-escaping bug
117. **Third e-commerce demo, explicitly required to use different techniques than both prior builds.** FERUS. = hover-swap grid + marquee + side drawer. MERIDIAN. = drag-carousel + quick-view panel + video hero + side drawer. FIELDWORK. = scroll-jacked horizontal product rail (GSAP `ScrollTrigger`-pinned, `x`-translated by scroll progress with a visible progress bar) + hotspot/pin product image (click a pin on a lifestyle shot to reveal a mini add-to-cart card) + bottom-sheet cart (slides up from the bottom edge, not a side drawer). Three different content-browsing interactions, three different cart-container UI patterns, across three builds — confirms the skill's technique catalog isn't limited to what's already been used once.
118. **Real bug: a double-escaped apostrophe inside a single-quoted JS string literal (`'You\\'re on the list...'`) silently broke ALL subsequent script execution on the page**, not just the one line. The double backslash (`\\`) is itself a valid escape producing one literal backslash character, so the string actually closed at the very next `'` (right after the literal backslash), leaving `re on the list — thanks.';` as bare, invalid syntax — a real `SyntaxError`/`Unexpected identifier` thrown at parse time, before any code in that `<script>` block runs. Because this project inlines all JS in one `<script>` tag (per the single-file delivery lesson, gotcha #107/108/110), a parse-time error anywhere in that block takes down every feature on the page at once — cart, hotspots, motion, everything — not just the newsletter form it was written for. Caught immediately by the QA harness's `page.on('pageerror')` listener during the very first scroll-walk pass, before any manual interaction testing even started. **Lesson**: when hand-authoring JS string literals containing an apostrophe inside single quotes, use a single backslash (`\'`) or just switch to double quotes (`"You're..."`) — a doubled backslash is a different, wrong escape, and because this file has no linter/bundler in the loop, this class of typo has no safety net except the pageerror check, which makes running that check non-optional on every build, not just ones that "seem risky."
119. **Same root typo pattern, lower severity, found in the same pass**: `'\\n\\n'` (meant as two escaped newlines inside a JS string) actually produced the literal 4-character text `\n\n` printed inside an `alert()`, instead of two real line breaks — a cosmetic bug, not a crash, because it didn't touch the string's closing quote. Both instances came from the same authoring mistake (typing `\\` when a literal single backslash-escape `\n`/`\'` was intended) — worth a mental note that in a raw (non-templated) JS string written directly into an HTML file, `\\` and `\` are NOT interchangeable stylistic choices, they mean different things, unlike in some other quoting/escaping contexts used elsewhere in these builds (e.g. shell heredocs).

## Twenty-seventh pass — FIELDWORK. rejected wholesale except one mechanic; rebuilt as ARLO., a real sub-4.5:1 rendered-vs-calculated contrast gap
120. **Direct client rejection of an entire build except one specific interaction, not a request for incremental fixes.** Verbatim: "the only thing I like from that is when you scroll down it scrolls the slider besides that everything looked trash." This is a materially different signal than the earlier "don't like the sidebar" / "no animations at all" feedback rounds (those asked for specific swaps within a kept structure) — here almost nothing survives except one named mechanic (the horizontal scroll-jack). **Handled by**: confirming scope precisely via `AskUserQuestion` before rebuilding (which specific pieces besides visual style were also disliked — hotspot pins and layout/spacing were both called out; bottom-sheet cart was NOT flagged and was kept), then deleting the rejected build entirely rather than leaving a dead draft in the repo, and starting the new build (`arlo-ceramics/`) with a genuinely different business concept and palette rather than reskinning the same copy in new colors — a full rebuild reads as more considered than a coat of paint on the same rejected bones.
121. **A token that passed an independent Python luminance-formula contrast check (4.41:1, rounds to a pass at 4.5) failed the real browser-rendered axe scan at 4.4:1** — the WCAG relative-luminance formula itself is exact, but font sub-pixel rendering/anti-aliasing in the actual browser paint can shift the effectively-perceived contrast at the margin. **Lesson**: treat any token landing within ~0.1 of a WCAG threshold from an offline/script-based contrast check as NOT verified — only a real rendered-page axe scan (or a deliberately generous manual margin, e.g. targeting 5:1+ when the requirement is 4.5:1) should be trusted for values this close to the line. Fixed by darkening `--olive` from `#6B7353` to `#5A6146` (5.73:1, well clear of the threshold rather than skimming it).

## Twenty-eighth pass — restaurant build (Rowan &amp; Vine wine bar), systemic amber-on-dark contrast failure caught by having a dedicated dark-bg token
122. **First restaurant build since Trattoria Marchetti (`trattoria-awardssite`), deliberately different register**: neighborhood wine bar (moody forest green + ivory + amber, DM Serif Display) instead of Trattoria's photo-hero + draggable-gallery approach. Reservation form (name/date/time/party/email + honeypot) as the primary interactive surface, no cart/e-commerce logic — restaurants live and die on "reserve" not "buy."
123. **Systemic contrast bug: a single amber token (#B8823D) used correctly against ivory (4.16:1, borderline-safe) was reused as small text against a forest-dark background at four separate call sites (hours-strip labels, wine-section eyebrow, wine-list prices, footer italic ampersand) where it measured 3.94:1 — a fail at all four spots simultaneously.** Same shape as gotchas #104 and #116 but with a new twist: this build had already documented "amber only decorative/large on ivory" in the palette-audit pass, then the token was reused reflexively for dark-bg small text anyway during authoring, without re-checking. **Fixed** by adding a genuinely separate `--amber-lite` (#D9A66E, 6.03:1 on forest) explicitly named for dark-bg use and swapping all four call sites to it, rather than trying to compromise one shared token. **Lesson worth generalizing across every future build**: any accent color used against BOTH a light and a dark background needs TWO named tokens by default (`--accent` for one polarity, `--accent-alt` for the other), not one token stretched across both — the color that works optically on ivory almost never works on the paired dark, and the reverse. Don't wait for axe to force this pattern; declare both tokens up front when the palette is chosen.
124. **Second contrast-margin miss in the same build**: the footer bottom's `#9C9484` on forest measured 4.37:1 (fail), fixed to `#AAA294` (5.2:1). Same lesson as gotcha #121 — this color was originally picked by eye, not calc-checked, and would have needed a fix even if the offline calc had run. The two together (gotcha #123's systemic amber miss + this one) confirm the "declare accent-on-light AND accent-on-dark up front" rule from #123 needs to extend to every non-primary color in the palette, not just the loudest accent.

## Twenty-ninth pass — skill upgrade: taste profile, visual self-review, component library, imagery pipeline
125. **The skill now has four new standing structures, born from a gap analysis Kallon requested ("I want the peak of your capabilities")**: (a) `references/kallon-taste.md` — an append-only ledger of every real accept/reject with a profile summary, read before choosing any direction and updated after every reaction, because taste calls left in chat history die with the session; (b) `references/visual-review.md` — a mandatory Step 4.5 "ugly check" (screenshot 4+ scroll positions and answer a written checklist), because the two worst misses in project history (squished WebGL #97, the FIELDWORK rejection #120) passed every automated gate; (c) `references/component-library.md` — proven components indexed by CANONICAL SOURCE FILE (pointers, not snippets — snippets rot, gotcha #109; the live file is the only copy guaranteed to carry every fix), with the rule that improving a component makes the new build canonical and the pointer moves in the same commit; (d) an operational image-plan section in `imagery-direction.md` — imagery is decided at intake with one up-front cost approval for a 3–5 shot set in one prompt family, because placeholder gradients are the #1 mockup tell.
126. **The new visual-review protocol paid for itself on its first run**: reviewing Rowan &amp; Vine's screenshots (a build that had already passed the full automated harness at 0 violations) surfaced that the desktop hero's `align-items:end` left the top-left third of the viewport dead-empty above the eyebrow — a lopsided, bottom-heavy composition invisible to axe/overflow/pageerror checks. One-line fix (`align-items:center`), re-screenshot confirmed balanced. **Lesson**: composition/alignment choices that look fine in code (`align-items:end` "anchors the text to the image base" sounded intentional) can render as dead space — only the rendered pixels tell you, which is exactly why Step 4.5 exists and why it re-screenshots after every fix.
